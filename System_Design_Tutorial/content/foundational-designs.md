# Foundational Designs

## Overview
Welcome to the module on **Foundational Designs**. This page covers the following related subtopics:

- URL Shortener (TinyURL/bit.ly) — hash + base62, collision handling, analytics
- Distributed Cache (Redis cluster) — consistent hashing, eviction, replication
- Key-Value Store — LSM tree, compaction, range queries
- Content Delivery Network — pull vs push, origin shield, cache invalidation
- Rate Limiter (distributed) — token bucket in Redis, per-user limits
- Distributed Job Scheduler — priority queues, at-least-once execution, dedup

---

## URL Shortener (TinyURL/bit.ly) — hash + base62, collision handling, analytics

Detailed content for **URL Shortener (TinyURL/bit.ly) — hash + base62, collision handling, analytics** is currently being formulated. Check back soon!

## Distributed Cache (Redis cluster) — consistent hashing, eviction, replication

Detailed content for **Distributed Cache (Redis cluster) — consistent hashing, eviction, replication** is currently being formulated. Check back soon!

## Key-Value Store — LSM tree, compaction, range queries

Detailed content for **Key-Value Store — LSM tree, compaction, range queries** is currently being formulated. Check back soon!

## Content Delivery Network — pull vs push, origin shield, cache invalidation

Detailed content for **Content Delivery Network — pull vs push, origin shield, cache invalidation** is currently being formulated. Check back soon!

## Rate Limiter (distributed) — token bucket in Redis, per-user limits

Detailed content for **Rate Limiter (distributed) — token bucket in Redis, per-user limits** is currently being formulated. Check back soon!

## Distributed Job Scheduler — priority queues, at-least-once execution, dedup

Detailed content for **Distributed Job Scheduler — priority queues, at-least-once execution, dedup** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
