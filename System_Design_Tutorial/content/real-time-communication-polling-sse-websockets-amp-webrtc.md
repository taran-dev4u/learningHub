# Real-Time Communication: Polling, SSE, WebSockets &amp; WebRTC

## Overview
Welcome to the module on **Real-Time Communication: Polling, SSE, WebSockets &amp; WebRTC**. This page covers the following related subtopics:

- Short polling — client requests every N seconds, wasteful
- Long polling — server holds connection until data ready or timeout
- SSE (Server-Sent Events) — HTTP-based, server→client only, auto-reconnect
- WebSockets — full-duplex TCP, use for chat, real-time games, collaboration
- WebRTC — peer-to-peer media, used for video calls (Zoom, Meet)
- Scaling WebSockets — sticky LB or message broker fan-out (Redis Pub/Sub)

---

## Short polling — client requests every N seconds, wasteful

Detailed content for **Short polling — client requests every N seconds, wasteful** is currently being formulated. Check back soon!

## Long polling — server holds connection until data ready or timeout

Detailed content for **Long polling — server holds connection until data ready or timeout** is currently being formulated. Check back soon!

## SSE (Server-Sent Events) — HTTP-based, server→client only, auto-reconnect

Detailed content for **SSE (Server-Sent Events) — HTTP-based, server→client only, auto-reconnect** is currently being formulated. Check back soon!

## WebSockets — full-duplex TCP, use for chat, real-time games, collaboration

Detailed content for **WebSockets — full-duplex TCP, use for chat, real-time games, collaboration** is currently being formulated. Check back soon!

## WebRTC — peer-to-peer media, used for video calls (Zoom, Meet)

Detailed content for **WebRTC — peer-to-peer media, used for video calls (Zoom, Meet)** is currently being formulated. Check back soon!

## Scaling WebSockets — sticky LB or message broker fan-out (Redis Pub/Sub)

Detailed content for **Scaling WebSockets — sticky LB or message broker fan-out (Redis Pub/Sub)** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
