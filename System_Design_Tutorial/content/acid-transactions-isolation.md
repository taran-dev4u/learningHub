# ACID, Transactions & Isolation

## Overview
Welcome to the module on **ACID, Transactions & Isolation**.

## Atomicity — all-or-nothing, rollback on failure

Detailed content for **Atomicity — all-or-nothing, rollback on failure** is currently being formulated.

## Consistency — valid state to valid state (app-defined invariants)

Detailed content for **Consistency — valid state to valid state (app-defined invariants)** is currently being formulated.

## Isolation — concurrent txns appear serial (isolation levels control this)

Detailed content for **Isolation — concurrent txns appear serial (isolation levels control this)** is currently being formulated.

## Durability — committed txns survive crashes (WAL, fsync)

Detailed content for **Durability — committed txns survive crashes (WAL, fsync)** is currently being formulated.

## Isolation levels: Read Uncommitted → Committed → Repeatable Read → Serializable

Detailed content for **Isolation levels: Read Uncommitted → Committed → Repeatable Read → Serializable** is currently being formulated.

## MVCC (Postgres, Oracle) — snapshot reads, no read locks

Detailed content for **MVCC (Postgres, Oracle) — snapshot reads, no read locks** is currently being formulated.

## BASE model — Basically Available, Soft state, Eventual consistency (NoSQL alternative)

Detailed content for **BASE model — Basically Available, Soft state, Eventual consistency (NoSQL alternative)** is currently being formulated.

