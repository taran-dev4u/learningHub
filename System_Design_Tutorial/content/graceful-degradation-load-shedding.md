# Graceful Degradation & Load Shedding

## Overview
Welcome to the module on **Graceful Degradation & Load Shedding**.

## Fallback responses — return stale cache or default when upstream is down

Detailed content for **Fallback responses — return stale cache or default when upstream is down** is currently being formulated.

## Feature flags / kill switches — turn off expensive features under load

Detailed content for **Feature flags / kill switches — turn off expensive features under load** is currently being formulated.

## Load shedding — reject low-priority requests early (return 503) before overload

Detailed content for **Load shedding — reject low-priority requests early (return 503) before overload** is currently being formulated.

## Priority queues — critical work executes even when system is degraded

Detailed content for **Priority queues — critical work executes even when system is degraded** is currently being formulated.

