# Redis &amp; Distributed Caching

## Overview
Welcome to the module on **Redis &amp; Distributed Caching**. This page covers the following related subtopics:

- Redis data types: strings, lists, sets, sorted sets, hashes, streams
- Redis cluster — consistent hash ring, 16384 slots, automatic sharding
- Redlock — distributed lock algorithm using multiple Redis instances
- Redis Pub/Sub — fan-out to WebSocket servers for scaling real-time
- Redis sorted sets — use for leaderboards, rate limiting with sliding window

---

## Redis data types: strings, lists, sets, sorted sets, hashes, streams

Detailed content for **Redis data types: strings, lists, sets, sorted sets, hashes, streams** is currently being formulated. Check back soon!

## Redis cluster — consistent hash ring, 16384 slots, automatic sharding

Detailed content for **Redis cluster — consistent hash ring, 16384 slots, automatic sharding** is currently being formulated. Check back soon!

## Redlock — distributed lock algorithm using multiple Redis instances

Detailed content for **Redlock — distributed lock algorithm using multiple Redis instances** is currently being formulated. Check back soon!

## Redis Pub/Sub — fan-out to WebSocket servers for scaling real-time

Detailed content for **Redis Pub/Sub — fan-out to WebSocket servers for scaling real-time** is currently being formulated. Check back soon!

## Redis sorted sets — use for leaderboards, rate limiting with sliding window

Detailed content for **Redis sorted sets — use for leaderboards, rate limiting with sliding window** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
