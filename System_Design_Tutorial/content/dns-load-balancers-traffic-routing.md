# DNS, Load Balancers & Traffic Routing

## Overview
Welcome to the module on **DNS, Load Balancers & Traffic Routing**.

## DNS resolution: recursive resolver → root → TLD → authoritative

Detailed content for **DNS resolution: recursive resolver → root → TLD → authoritative** is currently being formulated.

## DNS record types: A, AAAA, CNAME, MX, TXT, NS

Detailed content for **DNS record types: A, AAAA, CNAME, MX, TXT, NS** is currently being formulated.

## GeoDNS / latency-based routing (Route 53 / Cloudflare)

Detailed content for **GeoDNS / latency-based routing (Route 53 / Cloudflare)** is currently being formulated.

## L4 (TCP) load balancer — fast, blind to HTTP content

Detailed content for **L4 (TCP) load balancer — fast, blind to HTTP content** is currently being formulated.

## L7 (HTTP) load balancer — URL routing, sticky sessions, header inspection

Detailed content for **L7 (HTTP) load balancer — URL routing, sticky sessions, header inspection** is currently being formulated.

## LB algorithms: round robin, least-conn, IP hash, consistent hash, weighted

Detailed content for **LB algorithms: round robin, least-conn, IP hash, consistent hash, weighted** is currently being formulated.

## Health checks — active (ping) vs passive (track error rates)

Detailed content for **Health checks — active (ping) vs passive (track error rates)** is currently being formulated.

## Reverse proxy — single ingress, SSL termination, request buffering

Detailed content for **Reverse proxy — single ingress, SSL termination, request buffering** is currently being formulated.

