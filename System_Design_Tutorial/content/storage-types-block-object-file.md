# Storage Types: Block, Object & File

## Overview
Welcome to the module on **Storage Types: Block, Object & File**.

## Block storage (EBS, Azure Disk) — attach to one VM, high IOPS, DBs/OS disks

Detailed content for **Block storage (EBS, Azure Disk) — attach to one VM, high IOPS, DBs/OS disks** is currently being formulated.

## File storage (EFS, NFS, Azure Files) — multi-mount, POSIX semantics

Detailed content for **File storage (EFS, NFS, Azure Files) — multi-mount, POSIX semantics** is currently being formulated.

## Object storage (S3, GCS, Azure Blob) — flat namespace, infinite scale, HTTP API

Detailed content for **Object storage (S3, GCS, Azure Blob) — flat namespace, infinite scale, HTTP API** is currently being formulated.

## S3 key design — prefix distribution for parallel requests (avoid sequential prefixes)

Detailed content for **S3 key design — prefix distribution for parallel requests (avoid sequential prefixes)** is currently being formulated.

## Distributed file system (HDFS, Ceph) — large dataset processing, MapReduce

Detailed content for **Distributed file system (HDFS, Ceph) — large dataset processing, MapReduce** is currently being formulated.

