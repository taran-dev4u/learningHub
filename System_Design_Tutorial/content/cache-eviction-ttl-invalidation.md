# Cache Eviction, TTL & Invalidation

## Overview
Welcome to the module on **Cache Eviction, TTL & Invalidation**.

## LRU (Least Recently Used) — default Redis policy, good for most workloads

Detailed content for **LRU (Least Recently Used) — default Redis policy, good for most workloads** is currently being formulated.

## LFU (Least Frequently Used) — better when hot items are consistently hot

Detailed content for **LFU (Least Frequently Used) — better when hot items are consistently hot** is currently being formulated.

## TTL-based expiry — simplest, natural for session data

Detailed content for **TTL-based expiry — simplest, natural for session data** is currently being formulated.

## FIFO — first item cached, first evicted

Detailed content for **FIFO — first item cached, first evicted** is currently being formulated.

