# Real-Time Communication: Polling, SSE, WebSockets & WebRTC

## Overview
Welcome to the module on **Real-Time Communication: Polling, SSE, WebSockets & WebRTC**.

## Short polling — client requests every N seconds, wasteful

Detailed content for **Short polling — client requests every N seconds, wasteful** is currently being formulated.

## Long polling — server holds connection until data ready or timeout

Detailed content for **Long polling — server holds connection until data ready or timeout** is currently being formulated.

## SSE (Server-Sent Events) — HTTP-based, server→client only, auto-reconnect

Detailed content for **SSE (Server-Sent Events) — HTTP-based, server→client only, auto-reconnect** is currently being formulated.

## WebSockets — full-duplex TCP, use for chat, real-time games, collaboration

Detailed content for **WebSockets — full-duplex TCP, use for chat, real-time games, collaboration** is currently being formulated.

## WebRTC — peer-to-peer media, used for video calls (Zoom, Meet)

Detailed content for **WebRTC — peer-to-peer media, used for video calls (Zoom, Meet)** is currently being formulated.

## Scaling WebSockets — sticky LB or message broker fan-out (Redis Pub/Sub)

Detailed content for **Scaling WebSockets — sticky LB or message broker fan-out (Redis Pub/Sub)** is currently being formulated.

