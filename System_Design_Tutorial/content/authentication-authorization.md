# Authentication & Authorization

## Overview
Welcome to the module on **Authentication & Authorization**.

## Sessions (server-side state) vs JWT (stateless signed tokens)

Detailed content for **Sessions (server-side state) vs JWT (stateless signed tokens)** is currently being formulated.

## OAuth 2.0 — delegated authorization; flows: Auth Code, PKCE, Client Credentials

Detailed content for **OAuth 2.0 — delegated authorization; flows: Auth Code, PKCE, Client Credentials** is currently being formulated.

## OpenID Connect (OIDC) — authentication layer on top of OAuth 2.0

Detailed content for **OpenID Connect (OIDC) — authentication layer on top of OAuth 2.0** is currently being formulated.

## SAML — XML-based SSO, common in enterprise

Detailed content for **SAML — XML-based SSO, common in enterprise** is currently being formulated.

## MFA / WebAuthn — phishing-resistant hardware key, FIDO2

Detailed content for **MFA / WebAuthn — phishing-resistant hardware key, FIDO2** is currently being formulated.

## RBAC (Role-Based) vs ABAC (Attribute-Based) access control

Detailed content for **RBAC (Role-Based) vs ABAC (Attribute-Based) access control** is currently being formulated.

