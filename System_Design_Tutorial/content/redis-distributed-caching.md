# Redis & Distributed Caching

## Overview
Welcome to the module on **Redis & Distributed Caching**.

## Redis data types: strings, lists, sets, sorted sets, hashes, streams

Detailed content for **Redis data types: strings, lists, sets, sorted sets, hashes, streams** is currently being formulated.

## Redis cluster — consistent hash ring, 16384 slots, automatic sharding

Detailed content for **Redis cluster — consistent hash ring, 16384 slots, automatic sharding** is currently being formulated.

## Redlock — distributed lock algorithm using multiple Redis instances

Detailed content for **Redlock — distributed lock algorithm using multiple Redis instances** is currently being formulated.

## Redis Pub/Sub — fan-out to WebSocket servers for scaling real-time

Detailed content for **Redis Pub/Sub — fan-out to WebSocket servers for scaling real-time** is currently being formulated.

## Redis sorted sets — use for leaderboards, rate limiting with sliding window

Detailed content for **Redis sorted sets — use for leaderboards, rate limiting with sliding window** is currently being formulated.

