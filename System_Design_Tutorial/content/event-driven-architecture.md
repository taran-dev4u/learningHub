# Event-Driven Architecture

## Overview
Welcome to the module on **Event-Driven Architecture**. This page covers the following related subtopics:

- Event sourcing — state = fold(all events), full audit log, replayable
- CQRS — separate write model (commands) from read model (queries)
- SAGA pattern — chain of local txns + compensating actions (replaces 2PC)
- Choreography (events) vs Orchestration (central coordinator)
- Transactional Outbox — atomically write DB + outbox row, publish separately

---

## Event sourcing — state = fold(all events), full audit log, replayable

Detailed content for **Event sourcing — state = fold(all events), full audit log, replayable** is currently being formulated. Check back soon!

## CQRS — separate write model (commands) from read model (queries)

Detailed content for **CQRS — separate write model (commands) from read model (queries)** is currently being formulated. Check back soon!

## SAGA pattern — chain of local txns + compensating actions (replaces 2PC)

Detailed content for **SAGA pattern — chain of local txns + compensating actions (replaces 2PC)** is currently being formulated. Check back soon!

## Choreography (events) vs Orchestration (central coordinator)

Detailed content for **Choreography (events) vs Orchestration (central coordinator)** is currently being formulated. Check back soon!

## Transactional Outbox — atomically write DB + outbox row, publish separately

Detailed content for **Transactional Outbox — atomically write DB + outbox row, publish separately** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
