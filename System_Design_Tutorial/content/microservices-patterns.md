# Microservices Patterns

## Overview
Welcome to the module on **Microservices Patterns**. This page covers the following related subtopics:

- API Gateway — single entry, auth, rate limit, routing, aggregation
- Service discovery — client-side (Eureka) vs server-side (Consul, K8s DNS)
- Service mesh (Istio, Linkerd) — sidecar proxies handle mTLS, retries, LB
- Sidecar pattern — helper container alongside main app for cross-cutting concerns
- Database-per-service — each service owns its schema, no shared DBs
- Strangler Fig — incrementally replace monolith by routing slices to new services
- Backends for Frontends (BFF) — separate API gateway per client type

---

## API Gateway — single entry, auth, rate limit, routing, aggregation

Detailed content for **API Gateway — single entry, auth, rate limit, routing, aggregation** is currently being formulated. Check back soon!

## Service discovery — client-side (Eureka) vs server-side (Consul, K8s DNS)

Detailed content for **Service discovery — client-side (Eureka) vs server-side (Consul, K8s DNS)** is currently being formulated. Check back soon!

## Service mesh (Istio, Linkerd) — sidecar proxies handle mTLS, retries, LB

Detailed content for **Service mesh (Istio, Linkerd) — sidecar proxies handle mTLS, retries, LB** is currently being formulated. Check back soon!

## Sidecar pattern — helper container alongside main app for cross-cutting concerns

Detailed content for **Sidecar pattern — helper container alongside main app for cross-cutting concerns** is currently being formulated. Check back soon!

## Database-per-service — each service owns its schema, no shared DBs

Detailed content for **Database-per-service — each service owns its schema, no shared DBs** is currently being formulated. Check back soon!

## Strangler Fig — incrementally replace monolith by routing slices to new services

Detailed content for **Strangler Fig — incrementally replace monolith by routing slices to new services** is currently being formulated. Check back soon!

## Backends for Frontends (BFF) — separate API gateway per client type

Detailed content for **Backends for Frontends (BFF) — separate API gateway per client type** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
