# HTTP, TLS & Protocol Fundamentals

## Overview
Welcome to the module on **HTTP, TLS & Protocol Fundamentals**.

## HTTP methods: GET/PUT/DELETE (idempotent) vs POST/PATCH (not idempotent)

Detailed content for **HTTP methods: GET/PUT/DELETE (idempotent) vs POST/PATCH (not idempotent)** is currently being formulated.

## Status codes: 2xx success · 3xx redirect · 4xx client · 5xx server

Detailed content for **Status codes: 2xx success · 3xx redirect · 4xx client · 5xx server** is currently being formulated.

## HTTP/1.1 — text, one request per connection, head-of-line blocking

Detailed content for **HTTP/1.1 — text, one request per connection, head-of-line blocking** is currently being formulated.

## HTTP/2 — binary framing, multiplexing, header compression (HPACK)

Detailed content for **HTTP/2 — binary framing, multiplexing, header compression (HPACK)** is currently being formulated.

## HTTP/3 — UDP-based QUIC, 0-RTT, no TCP HOL blocking

Detailed content for **HTTP/3 — UDP-based QUIC, 0-RTT, no TCP HOL blocking** is currently being formulated.

## TLS 1.3 handshake — 1 RTT, forward secrecy

Detailed content for **TLS 1.3 handshake — 1 RTT, forward secrecy** is currently being formulated.

