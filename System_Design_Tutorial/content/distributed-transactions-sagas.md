# Distributed Transactions & Sagas

## Overview
Welcome to the module on **Distributed Transactions & Sagas**.

## Two-Phase Commit (2PC) — prepare + commit phases, blocks on coordinator failure

Detailed content for **Two-Phase Commit (2PC) — prepare + commit phases, blocks on coordinator failure** is currently being formulated.

## SAGA — chain of local txns + compensating actions, no distributed lock

Detailed content for **SAGA — chain of local txns + compensating actions, no distributed lock** is currently being formulated.

## Transactional Outbox — write DB + outbox atomically, publish from outbox

Detailed content for **Transactional Outbox — write DB + outbox atomically, publish from outbox** is currently being formulated.

## Vector clocks — track causality, detect concurrent writes

Detailed content for **Vector clocks — track causality, detect concurrent writes** is currently being formulated.

