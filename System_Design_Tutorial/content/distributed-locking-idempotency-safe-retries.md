# Distributed Locking, Idempotency & Safe Retries

## Overview
Welcome to the module on **Distributed Locking, Idempotency & Safe Retries**.

## Distributed lock use cases — prevent double-processing, inventory decrement

Detailed content for **Distributed lock use cases — prevent double-processing, inventory decrement** is currently being formulated.

## Redlock — Redis-based algorithm using quorum of N nodes

Detailed content for **Redlock — Redis-based algorithm using quorum of N nodes** is currently being formulated.

## ZooKeeper / etcd for distributed coordination (more robust than Redlock)

Detailed content for **ZooKeeper / etcd for distributed coordination (more robust than Redlock)** is currently being formulated.

## Optimistic locking (version columns) — retry on conflict, no lock held

Detailed content for **Optimistic locking (version columns) — retry on conflict, no lock held** is currently being formulated.

