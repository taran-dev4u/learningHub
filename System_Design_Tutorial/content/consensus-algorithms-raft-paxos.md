# Consensus Algorithms: Raft & Paxos

## Overview
Welcome to the module on **Consensus Algorithms: Raft & Paxos**.

## Raft — leader election, log replication, safety guarantees (easier than Paxos)

Detailed content for **Raft — leader election, log replication, safety guarantees (easier than Paxos)** is currently being formulated.

## Raft leader election — candidate, follower, leader roles; randomized timeouts

Detailed content for **Raft leader election — candidate, follower, leader roles; randomized timeouts** is currently being formulated.

## Log replication — leader appends, replicates to quorum, commits

Detailed content for **Log replication — leader appends, replicates to quorum, commits** is currently being formulated.

## Paxos — original consensus algorithm, harder to understand and implement

Detailed content for **Paxos — original consensus algorithm, harder to understand and implement** is currently being formulated.

## Used in: etcd (Kubernetes), ZooKeeper, CockroachDB, MongoDB (replica sets)

Detailed content for **Used in: etcd (Kubernetes), ZooKeeper, CockroachDB, MongoDB (replica sets)** is currently being formulated.

