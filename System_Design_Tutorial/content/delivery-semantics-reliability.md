# Delivery Semantics & Reliability

## Overview
Welcome to the module on **Delivery Semantics & Reliability**.

## At-most-once — fire and forget, may lose messages

Detailed content for **At-most-once — fire and forget, may lose messages** is currently being formulated.

## At-least-once — retry on failure, duplicates possible → make consumers idempotent

Detailed content for **At-least-once — retry on failure, duplicates possible → make consumers idempotent** is currently being formulated.

## Exactly-once — Kafka transactions or idempotency keys

Detailed content for **Exactly-once — Kafka transactions or idempotency keys** is currently being formulated.

## Idempotency keys — safe retries, critical for payments (Airbnb, Stripe)

Detailed content for **Idempotency keys — safe retries, critical for payments (Airbnb, Stripe)** is currently being formulated.

## Dead Letter Queue (DLQ) — park failed messages after N retries for inspection

Detailed content for **Dead Letter Queue (DLQ) — park failed messages after N retries for inspection** is currently being formulated.

