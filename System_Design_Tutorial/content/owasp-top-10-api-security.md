# OWASP Top 10 & API Security

## Overview
Welcome to the module on **OWASP Top 10 & API Security**.

## SQL/NoSQL/OS injection — user input as code; fix: parameterized queries

Detailed content for **SQL/NoSQL/OS injection — user input as code; fix: parameterized queries** is currently being formulated.

## XSS (Cross-Site Scripting) — injected JS in pages; fix: escape output, CSP

Detailed content for **XSS (Cross-Site Scripting) — injected JS in pages; fix: escape output, CSP** is currently being formulated.

## CSRF (Cross-Site Request Forgery) — forged requests; fix: CSRF tokens, SameSite

Detailed content for **CSRF (Cross-Site Request Forgery) — forged requests; fix: CSRF tokens, SameSite** is currently being formulated.

## SSRF (Server-Side Request Forgery) — server fetches internal URLs; exploits cloud metadata

Detailed content for **SSRF (Server-Side Request Forgery) — server fetches internal URLs; exploits cloud metadata** is currently being formulated.

## Broken authentication — weak passwords, no MFA, exposed session IDs

Detailed content for **Broken authentication — weak passwords, no MFA, exposed session IDs** is currently being formulated.

## API security: always authenticate, validate input, log all calls, rate limit

Detailed content for **API security: always authenticate, validate input, log all calls, rate limit** is currently being formulated.

