# Distributed Transactions &amp; Sagas

## Overview
Welcome to the module on **Distributed Transactions &amp; Sagas**. This page covers the following related subtopics:

- Two-Phase Commit (2PC) — prepare + commit phases, blocks on coordinator failure
- SAGA — chain of local txns + compensating actions, no distributed lock
- Transactional Outbox — write DB + outbox atomically, publish from outbox
- Vector clocks — track causality, detect concurrent writes

---

## Two-Phase Commit (2PC) — prepare + commit phases, blocks on coordinator failure

Detailed content for **Two-Phase Commit (2PC) — prepare + commit phases, blocks on coordinator failure** is currently being formulated. Check back soon!

## SAGA — chain of local txns + compensating actions, no distributed lock

Detailed content for **SAGA — chain of local txns + compensating actions, no distributed lock** is currently being formulated. Check back soon!

## Transactional Outbox — write DB + outbox atomically, publish from outbox

Detailed content for **Transactional Outbox — write DB + outbox atomically, publish from outbox** is currently being formulated. Check back soon!

## Vector clocks — track causality, detect concurrent writes

Detailed content for **Vector clocks — track causality, detect concurrent writes** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
