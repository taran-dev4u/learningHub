# Logging & Distributed Tracing

## Overview
Welcome to the module on **Logging & Distributed Tracing**.

## Structured logs — JSON with trace_id, user_id, latency_ms on every line

Detailed content for **Structured logs — JSON with trace_id, user_id, latency_ms on every line** is currently being formulated.

## Distributed tracing — trace_id threads requests across services (Jaeger, Zipkin)

Detailed content for **Distributed tracing — trace_id threads requests across services (Jaeger, Zipkin)** is currently being formulated.

## W3C Trace Context standard — interoperable trace propagation headers

Detailed content for **W3C Trace Context standard — interoperable trace propagation headers** is currently being formulated.

## CloudTrail / Activity Log — audit trail for 'who did what when'

Detailed content for **CloudTrail / Activity Log — audit trail for 'who did what when'** is currently being formulated.

## Alert fatigue — every alert must be actionable or it becomes noise

Detailed content for **Alert fatigue — every alert must be actionable or it becomes noise** is currently being formulated.

