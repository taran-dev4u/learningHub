# Consensus Algorithms: Raft &amp; Paxos

## Overview
Welcome to the module on **Consensus Algorithms: Raft &amp; Paxos**. This page covers the following related subtopics:

- Raft — leader election, log replication, safety guarantees (easier than Paxos)
- Raft leader election — candidate, follower, leader roles; randomized timeouts
- Log replication — leader appends, replicates to quorum, commits
- Paxos — original consensus algorithm, harder to understand and implement
- Used in: etcd (Kubernetes), ZooKeeper, CockroachDB, MongoDB (replica sets)

---

## Raft — leader election, log replication, safety guarantees (easier than Paxos)

Detailed content for **Raft — leader election, log replication, safety guarantees (easier than Paxos)** is currently being formulated. Check back soon!

## Raft leader election — candidate, follower, leader roles; randomized timeouts

Detailed content for **Raft leader election — candidate, follower, leader roles; randomized timeouts** is currently being formulated. Check back soon!

## Log replication — leader appends, replicates to quorum, commits

Detailed content for **Log replication — leader appends, replicates to quorum, commits** is currently being formulated. Check back soon!

## Paxos — original consensus algorithm, harder to understand and implement

Detailed content for **Paxos — original consensus algorithm, harder to understand and implement** is currently being formulated. Check back soon!

## Used in: etcd (Kubernetes), ZooKeeper, CockroachDB, MongoDB (replica sets)

Detailed content for **Used in: etcd (Kubernetes), ZooKeeper, CockroachDB, MongoDB (replica sets)** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
