# Indexes & Query Optimization

## Overview
Welcome to the module on **Indexes & Query Optimization**.

## B-tree index — balanced tree, good for range + equality queries (default)

Detailed content for **B-tree index — balanced tree, good for range + equality queries (default)** is currently being formulated.

## Hash index — O(1) equality, no range queries

Detailed content for **Hash index — O(1) equality, no range queries** is currently being formulated.

## Composite index + leftmost prefix rule

Detailed content for **Composite index + leftmost prefix rule** is currently being formulated.

## Covering index — all needed cols in index, no table read

Detailed content for **Covering index — all needed cols in index, no table read** is currently being formulated.

## LSM tree (Cassandra, RocksDB) — write-optimized, sorted runs + compaction

Detailed content for **LSM tree (Cassandra, RocksDB) — write-optimized, sorted runs + compaction** is currently being formulated.

## EXPLAIN ANALYZE — read query plan, spot Seq Scans on big tables

Detailed content for **EXPLAIN ANALYZE — read query plan, spot Seq Scans on big tables** is currently being formulated.

## Index selectivity — high cardinality cols are good candidates

Detailed content for **Index selectivity — high cardinality cols are good candidates** is currently being formulated.

