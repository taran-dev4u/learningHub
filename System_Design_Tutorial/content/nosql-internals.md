# NoSQL Internals

## Overview
Welcome to the module on **NoSQL Internals**. This page covers the following related subtopics:

- Redis data structures: strings, hashes, sets, sorted sets, streams
- Cassandra ring topology, token ring, virtual nodes
- DynamoDB — partition key design, GSI, consistent reads vs eventually consistent
- MongoDB — documents, collections, embedded vs referenced, aggregation pipeline
- Bloom filter — probabilistic, check if key MIGHT exist, no false negatives
- HyperLogLog — approximate cardinality counting with tiny memory

---

## Redis data structures: strings, hashes, sets, sorted sets, streams

Detailed content for **Redis data structures: strings, hashes, sets, sorted sets, streams** is currently being formulated. Check back soon!

## Cassandra ring topology, token ring, virtual nodes

Detailed content for **Cassandra ring topology, token ring, virtual nodes** is currently being formulated. Check back soon!

## DynamoDB — partition key design, GSI, consistent reads vs eventually consistent

Detailed content for **DynamoDB — partition key design, GSI, consistent reads vs eventually consistent** is currently being formulated. Check back soon!

## MongoDB — documents, collections, embedded vs referenced, aggregation pipeline

Detailed content for **MongoDB — documents, collections, embedded vs referenced, aggregation pipeline** is currently being formulated. Check back soon!

## Bloom filter — probabilistic, check if key MIGHT exist, no false negatives

Detailed content for **Bloom filter — probabilistic, check if key MIGHT exist, no false negatives** is currently being formulated. Check back soon!

## HyperLogLog — approximate cardinality counting with tiny memory

Detailed content for **HyperLogLog — approximate cardinality counting with tiny memory** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
