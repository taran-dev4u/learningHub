# Circuit Breakers, Bulkheads &amp; Timeouts

## Overview
Welcome to the module on **Circuit Breakers, Bulkheads &amp; Timeouts**. This page covers the following related subtopics:

- Circuit breaker states: Closed (normal) → Open (failing) → Half-Open (testing)
- Bulkhead — isolate resource pools so one busy area can&#x27;t starve others
- Timeouts — never call external service without one, default short
- Exponential backoff + jitter — wait 2^n seconds + random jitter, prevents stampede
- Retry storm antipattern — all layers retrying simultaneously = multiplicative load

---

## Circuit breaker states: Closed (normal) → Open (failing) → Half-Open (testing)

Detailed content for **Circuit breaker states: Closed (normal) → Open (failing) → Half-Open (testing)** is currently being formulated. Check back soon!

## Bulkhead — isolate resource pools so one busy area can&#x27;t starve others

Detailed content for **Bulkhead — isolate resource pools so one busy area can&#x27;t starve others** is currently being formulated. Check back soon!

## Timeouts — never call external service without one, default short

Detailed content for **Timeouts — never call external service without one, default short** is currently being formulated. Check back soon!

## Exponential backoff + jitter — wait 2^n seconds + random jitter, prevents stampede

Detailed content for **Exponential backoff + jitter — wait 2^n seconds + random jitter, prevents stampede** is currently being formulated. Check back soon!

## Retry storm antipattern — all layers retrying simultaneously = multiplicative load

Detailed content for **Retry storm antipattern — all layers retrying simultaneously = multiplicative load** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
