# Consistent Hashing & Rebalancing

## Overview
Welcome to the module on **Consistent Hashing & Rebalancing**.

## Hash ring — nodes placed at positions, keys routed clockwise

Detailed content for **Hash ring — nodes placed at positions, keys routed clockwise** is currently being formulated.

## Virtual nodes — each server occupies multiple ring positions

Detailed content for **Virtual nodes — each server occupies multiple ring positions** is currently being formulated.

## Adding/removing node moves only 1/N keys (vs N keys in modulo hashing)

Detailed content for **Adding/removing node moves only 1/N keys (vs N keys in modulo hashing)** is currently being formulated.

## Used by: Cassandra, DynamoDB, Memcached, CDN routing

Detailed content for **Used by: Cassandra, DynamoDB, Memcached, CDN routing** is currently being formulated.

