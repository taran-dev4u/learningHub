# Circuit Breakers, Bulkheads & Timeouts

## Overview
Welcome to the module on **Circuit Breakers, Bulkheads & Timeouts**.

## Circuit breaker states: Closed (normal) → Open (failing) → Half-Open (testing)

Detailed content for **Circuit breaker states: Closed (normal) → Open (failing) → Half-Open (testing)** is currently being formulated.

## Bulkhead — isolate resource pools so one busy area can't starve others

Detailed content for **Bulkhead — isolate resource pools so one busy area can't starve others** is currently being formulated.

## Timeouts — never call external service without one, default short

Detailed content for **Timeouts — never call external service without one, default short** is currently being formulated.

## Exponential backoff + jitter — wait 2^n seconds + random jitter, prevents stampede

Detailed content for **Exponential backoff + jitter — wait 2^n seconds + random jitter, prevents stampede** is currently being formulated.

## Retry storm antipattern — all layers retrying simultaneously = multiplicative load

Detailed content for **Retry storm antipattern — all layers retrying simultaneously = multiplicative load** is currently being formulated.

