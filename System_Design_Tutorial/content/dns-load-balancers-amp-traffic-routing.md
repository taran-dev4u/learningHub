# DNS, Load Balancers &amp; Traffic Routing

## Overview
Welcome to the module on **DNS, Load Balancers &amp; Traffic Routing**. This page covers the following related subtopics:

- DNS resolution: recursive resolver → root → TLD → authoritative
- DNS record types: A, AAAA, CNAME, MX, TXT, NS
- GeoDNS / latency-based routing (Route 53 / Cloudflare)
- L4 (TCP) load balancer — fast, blind to HTTP content
- L7 (HTTP) load balancer — URL routing, sticky sessions, header inspection
- LB algorithms: round robin, least-conn, IP hash, consistent hash, weighted
- Health checks — active (ping) vs passive (track error rates)
- Reverse proxy — single ingress, SSL termination, request buffering

---

## DNS resolution: recursive resolver → root → TLD → authoritative

Detailed content for **DNS resolution: recursive resolver → root → TLD → authoritative** is currently being formulated. Check back soon!

## DNS record types: A, AAAA, CNAME, MX, TXT, NS

Detailed content for **DNS record types: A, AAAA, CNAME, MX, TXT, NS** is currently being formulated. Check back soon!

## GeoDNS / latency-based routing (Route 53 / Cloudflare)

Detailed content for **GeoDNS / latency-based routing (Route 53 / Cloudflare)** is currently being formulated. Check back soon!

## L4 (TCP) load balancer — fast, blind to HTTP content

Detailed content for **L4 (TCP) load balancer — fast, blind to HTTP content** is currently being formulated. Check back soon!

## L7 (HTTP) load balancer — URL routing, sticky sessions, header inspection

Detailed content for **L7 (HTTP) load balancer — URL routing, sticky sessions, header inspection** is currently being formulated. Check back soon!

## LB algorithms: round robin, least-conn, IP hash, consistent hash, weighted

Detailed content for **LB algorithms: round robin, least-conn, IP hash, consistent hash, weighted** is currently being formulated. Check back soon!

## Health checks — active (ping) vs passive (track error rates)

Detailed content for **Health checks — active (ping) vs passive (track error rates)** is currently being formulated. Check back soon!

## Reverse proxy — single ingress, SSL termination, request buffering

Detailed content for **Reverse proxy — single ingress, SSL termination, request buffering** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
