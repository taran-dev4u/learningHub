# Partitioning, Sharding &amp; Data Distribution

## Overview
Welcome to the module on **Partitioning, Sharding &amp; Data Distribution**. This page covers the following related subtopics:

- Range sharding — split by key ranges (e.g., ID 0-1M, 1M-2M)
- Hash sharding — hash(key) % N. Even distribution but no range queries
- Consistent hashing — resharding moves only 1/N keys
- Hot key problem — celebrity users, time-based skew, fix with salting
- Cross-shard joins — expensive, avoid by denormalizing or keeping related data together

---

## Range sharding — split by key ranges (e.g., ID 0-1M, 1M-2M)

Detailed content for **Range sharding — split by key ranges (e.g., ID 0-1M, 1M-2M)** is currently being formulated. Check back soon!

## Hash sharding — hash(key) % N. Even distribution but no range queries

Detailed content for **Hash sharding — hash(key) % N. Even distribution but no range queries** is currently being formulated. Check back soon!

## Consistent hashing — resharding moves only 1/N keys

Detailed content for **Consistent hashing — resharding moves only 1/N keys** is currently being formulated. Check back soon!

## Hot key problem — celebrity users, time-based skew, fix with salting

Detailed content for **Hot key problem — celebrity users, time-based skew, fix with salting** is currently being formulated. Check back soon!

## Cross-shard joins — expensive, avoid by denormalizing or keeping related data together

Detailed content for **Cross-shard joins — expensive, avoid by denormalizing or keeping related data together** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
