# Kafka Architecture

## Overview
Welcome to the module on **Kafka Architecture**. This page covers the following related subtopics:

- Topics, partitions, offsets — horizontal scale by adding partitions
- Consumer groups — each group sees all messages; within group, each partition → one consumer
- Sequential disk I/O + zero-copy — why Kafka is fast
- Retention — keep messages N days regardless of consumption
- Exactly-once semantics (Kafka transactions, idempotent producers)

---

## Topics, partitions, offsets — horizontal scale by adding partitions

Detailed content for **Topics, partitions, offsets — horizontal scale by adding partitions** is currently being formulated. Check back soon!

## Consumer groups — each group sees all messages; within group, each partition → one consumer

Detailed content for **Consumer groups — each group sees all messages; within group, each partition → one consumer** is currently being formulated. Check back soon!

## Sequential disk I/O + zero-copy — why Kafka is fast

Detailed content for **Sequential disk I/O + zero-copy — why Kafka is fast** is currently being formulated. Check back soon!

## Retention — keep messages N days regardless of consumption

Detailed content for **Retention — keep messages N days regardless of consumption** is currently being formulated. Check back soon!

## Exactly-once semantics (Kafka transactions, idempotent producers)

Detailed content for **Exactly-once semantics (Kafka transactions, idempotent producers)** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
