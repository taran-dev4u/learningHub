# HTTP, TLS &amp; Protocol Fundamentals

## Overview
Welcome to the module on **HTTP, TLS &amp; Protocol Fundamentals**. This page covers the following related subtopics:

- HTTP methods: GET/PUT/DELETE (idempotent) vs POST/PATCH (not idempotent)
- Status codes: 2xx success · 3xx redirect · 4xx client · 5xx server
- HTTP/1.1 — text, one request per connection, head-of-line blocking
- HTTP/2 — binary framing, multiplexing, header compression (HPACK)
- HTTP/3 — UDP-based QUIC, 0-RTT, no TCP HOL blocking
- TLS 1.3 handshake — 1 RTT, forward secrecy

---

## HTTP methods: GET/PUT/DELETE (idempotent) vs POST/PATCH (not idempotent)

Detailed content for **HTTP methods: GET/PUT/DELETE (idempotent) vs POST/PATCH (not idempotent)** is currently being formulated. Check back soon!

## Status codes: 2xx success · 3xx redirect · 4xx client · 5xx server

Detailed content for **Status codes: 2xx success · 3xx redirect · 4xx client · 5xx server** is currently being formulated. Check back soon!

## HTTP/1.1 — text, one request per connection, head-of-line blocking

Detailed content for **HTTP/1.1 — text, one request per connection, head-of-line blocking** is currently being formulated. Check back soon!

## HTTP/2 — binary framing, multiplexing, header compression (HPACK)

Detailed content for **HTTP/2 — binary framing, multiplexing, header compression (HPACK)** is currently being formulated. Check back soon!

## HTTP/3 — UDP-based QUIC, 0-RTT, no TCP HOL blocking

Detailed content for **HTTP/3 — UDP-based QUIC, 0-RTT, no TCP HOL blocking** is currently being formulated. Check back soon!

## TLS 1.3 handshake — 1 RTT, forward secrecy

Detailed content for **TLS 1.3 handshake — 1 RTT, forward secrecy** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
