# CDN Caching

## Overview
Welcome to the module on **CDN Caching**. This page covers the following related subtopics:

- Pull CDN — cache on first request (lazy), low setup, may have cold cache
- Push CDN — pre-upload content, guaranteed hot cache, high storage cost
- Cache invalidation — URL versioning or purge API
- CDN for dynamic content — cache HTML + API responses at edge

---

## Pull CDN — cache on first request (lazy), low setup, may have cold cache

Detailed content for **Pull CDN — cache on first request (lazy), low setup, may have cold cache** is currently being formulated. Check back soon!

## Push CDN — pre-upload content, guaranteed hot cache, high storage cost

Detailed content for **Push CDN — pre-upload content, guaranteed hot cache, high storage cost** is currently being formulated. Check back soon!

## Cache invalidation — URL versioning or purge API

Detailed content for **Cache invalidation — URL versioning or purge API** is currently being formulated. Check back soon!

## CDN for dynamic content — cache HTML + API responses at edge

Detailed content for **CDN for dynamic content — cache HTML + API responses at edge** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
