# API Styles: REST, GraphQL &amp; gRPC

## Overview
Welcome to the module on **API Styles: REST, GraphQL &amp; gRPC**. This page covers the following related subtopics:

- REST — stateless, resources + HTTP verbs, cacheable, uniform interface
- GraphQL — single endpoint, client picks fields, no over/under-fetching
- gRPC — HTTP/2 + Protobuf, bidirectional streaming, ideal for internal microservices
- API Gateway — auth, rate limiting, routing, transformation at ingress
- Idempotency keys — safe retries, critical for payments and mutations
- Webhooks — server pushes to registered callback URL on event

---

## REST — stateless, resources + HTTP verbs, cacheable, uniform interface

Detailed content for **REST — stateless, resources + HTTP verbs, cacheable, uniform interface** is currently being formulated. Check back soon!

## GraphQL — single endpoint, client picks fields, no over/under-fetching

Detailed content for **GraphQL — single endpoint, client picks fields, no over/under-fetching** is currently being formulated. Check back soon!

## gRPC — HTTP/2 + Protobuf, bidirectional streaming, ideal for internal microservices

Detailed content for **gRPC — HTTP/2 + Protobuf, bidirectional streaming, ideal for internal microservices** is currently being formulated. Check back soon!

## API Gateway — auth, rate limiting, routing, transformation at ingress

Detailed content for **API Gateway — auth, rate limiting, routing, transformation at ingress** is currently being formulated. Check back soon!

## Idempotency keys — safe retries, critical for payments and mutations

Detailed content for **Idempotency keys — safe retries, critical for payments and mutations** is currently being formulated. Check back soon!

## Webhooks — server pushes to registered callback URL on event

Detailed content for **Webhooks — server pushes to registered callback URL on event** is currently being formulated. Check back soon!

> [!NOTE]
> **Teacher's Note:** This is the *Light-Depth Baseline Version* of this tutorial. We will upgrade this page with deep-dives shortly!
